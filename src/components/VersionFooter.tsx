'use client';

import Link from 'next/link';
import { ExternalLink } from 'lucide-react';
import { usePathname } from 'next/navigation';

export default function VersionFooter() {
    // Footer removido - ya no se muestra
    return null;
}
